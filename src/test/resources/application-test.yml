spring:
  datasource:
    url: jdbc:tc:postgresql:16-alpine:///test

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

server:
  servlet:
    session:
      cookie:
        http-only: false
        same-site: none
        secure: false
