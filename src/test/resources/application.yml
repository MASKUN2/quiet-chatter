spring:
  data:
    redis:
      repositories:
        enabled: false
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        default_batch_fetch_size: 100

naver:
  api:
    client-id: hidden
    client-secret: hidden
jwt:
  secret-key: someveryverymuchlongtestsecretkey